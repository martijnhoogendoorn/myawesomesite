extends layout

block content
  h1= title
  p Welcome to the Amazing "#{title}" site on Microsoft Azure!

  p By the way, how about this appsetting? [#{process.env.ANODEJSAPPSETTING}]
  p On another note, how about this ConnectionString? [#{process.env.CUSTOMCONNSTR_ThisIsAnExampleConnectionString}]
  p What the heck, here's another one: [#{process.env.SQLCONNSTR_ThisIsASqlServerConnectionString}]